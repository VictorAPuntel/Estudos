/**Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente. */

const baseHighExpoent = (base, expoent) =>
  console.log(`Base high expoent: ${Math.pow(base, expoent)}`);
baseHighExpoent(2, 4);
